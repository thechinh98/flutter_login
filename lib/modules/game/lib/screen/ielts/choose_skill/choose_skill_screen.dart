import 'package:flutter/material.dart';

class ChooseSkillScreen extends StatelessWidget {
  const ChooseSkillScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
